APP_NAME=camgo
APP_BIN=/usr/local/bin/${APP_NAME}
APP_CONTAINER=${APP_NAME}-api

WORKDIR=/app
GO_VERSION=1.22
ALPINE_VERSION=3.19
PORT=8080
NETWORK_NAME=${APP_NAME}-net

DATABASE_DRIVER=postgres

# PostgreSQL - database
PGDB_SERVICE_NAME=pgdb
PGDB_CONTAINER_NAME=pgdb
PGDB_VERSION=16
PGDB_IMAGE=postgres:${PGDB_VERSION}-alpine${ALPINE_VERSION}

DATABASE_POSTGRES_HOST=localhost
DATABASE_POSTGRES_USER=${APP_NAME}user
DATABASE_POSTGRES_PASSWD=${APP_NAME}p44s5W0rD
DATABASE_POSTGRES_DBNAME=${APP_NAME}

# PgAdmin - default PostgreSQL WEB-GUI
PGADMIN_CONTAINER=pgadmin
PGADMIN_VERSION=latest
PGADMIN_IMAGE=dpage/pgadmin4:${PGADMIN_VERSION}

PGADMIN_EMAIL=admin@camgo.com
PGADMIN_PASSWD=c4mG0!4dm1n
PGADMIN_PORT=5050